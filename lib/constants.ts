/** Límite de productos por página en listado. */
export const PRODUCTS_PAGE_LIMIT = 24;

/** Píxeles de scroll para considerar "scrolled" en el header (cambio transparente → sólido). */
export const HEADER_SCROLL_THRESHOLD = 50;
